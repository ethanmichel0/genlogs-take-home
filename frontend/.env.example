# Browser-visible key. Restrict it by HTTP referrer and enabled Google APIs.
VITE_GOOGLE_MAPS_API_KEY=replace-with-a-browser-restricted-key

# Optional in local development because Vite defaults to this FastAPI URL.
# Required for the separate production frontend; set it to the deployed backend URL.
VITE_API_BASE_URL=http://localhost:8000
